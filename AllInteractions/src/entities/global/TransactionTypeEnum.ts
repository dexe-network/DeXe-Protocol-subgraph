import { BigInt } from "@graphprotocol/graph-ts";

export enum TransactionType {
  UPDATED_USER_CREDENTIALS = 1,

  USER_AGREED_TO_PRIVACY_POLICY = 2,

  DAO_POOL_CREATED = 3,
  DAO_POOL_PROPOSAL_CREATED = 4,
  DAO_POOL_PROPOSAL_VOTED = 5,
  DAO_POOL_PROPOSAL_EXECUTED = 6,

  DAO_POOL_DELEGATED = 7,
  DAO_POOL_UNDELEGATED = 8,
  DAO_POOL_REQUESTED = 9,
  DAO_POOL_REWARD_CLAIMED = 10,
  DAO_POOL_DEPOSITED = 11,
  DAO_POOL_WITHDRAWN = 12,
  DAO_POOL_MOVED_TO_VALIDATORS = 13,
  DAO_POOL_OFFCHAIN_RESULTS_SAVED = 14,

  DAO_VALIDATORS_VOTED = 15,
  DAO_VALIDATORS_PROPOSAL_CREATED = 16,
  DAO_VALIDATORS_PROPOSAL_EXECUTED = 17,
}

export function getEnumBigInt(operation: TransactionType): BigInt {
  return BigInt.fromI32(operation as i32);
}
